type Query {
  getAllData: GraphData
}

type GraphData {
  papers: [Paper]
  authors: [Author]
  links: [Link]
}

type Paper {
  id: String
  title: String
  publicationDate: String
  summary: String
  references: [Paper]
  incomingReferencesCount: Int
  source: String
}

type Author {
  id: String
  name: String
  papers: [Paper]
}

type Link {
  source: String
  target: String
  type: String
}
